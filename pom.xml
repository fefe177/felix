<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <groupId>eu.bieder</groupId>
    <artifactId>BigMC</artifactId>
    <version>0.1.0</version>
    <packaging>jar</packaging>

    <name>BigMC</name>
    <description>Survival-SMP Plugin (HugoSMP/DonutSMP-Stil)</description>

    <properties>
        <!-- Java 21 wie vorgegeben -->
        <java.version>21</java.version>
        <maven.compiler.release>21</maven.compiler.release>
        <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
    </properties>

    <repositories>
        <!-- PaperMC Repository fuer die Paper-API -->
        <repository>
            <id>papermc</id>
            <url>https://repo.papermc.io/repository/maven-public/</url>
        </repository>
        <!-- JitPack: stellt die NuVotifier-API bereit (Phase 9) -->
        <repository>
            <id>jitpack</id>
            <url>https://jitpack.io</url>
        </repository>
    </repositories>

    <dependencies>
        <!-- Paper API 1.21.x (nur zur Compile-Zeit, vom Server bereitgestellt) -->
        <dependency>
            <groupId>io.papermc.paper</groupId>
            <artifactId>paper-api</artifactId>
            <version>1.21.4-R0.1-SNAPSHOT</version>
            <scope>provided</scope>
        </dependency>

        <!-- SQLite JDBC Treiber - wird in die JAR geshadet (kein externer DB-Server noetig) -->
        <dependency>
            <groupId>org.xerial</groupId>
            <artifactId>sqlite-jdbc</artifactId>
            <version>3.47.1.0</version>
            <scope>compile</scope>
        </dependency>

        <!-- NuVotifier API (Phase 9) - nur zur Compile-Zeit, vom Vote-Plugin bereitgestellt.
             Soft-Depend: das Plugin laeuft auch ohne NuVotifier. -->
        <dependency>
            <groupId>com.github.NuVotifier.NuVotifier</groupId>
            <artifactId>nuvotifier-api</artifactId>
            <version>2.7.3</version>
            <scope>provided</scope>
        </dependency>
    </dependencies>

    <build>
        <!-- Ausgabename der fertigen Datei: BigMC.jar -->
        <finalName>BigMC</finalName>

        <plugins>
            <!-- Compiler-Plugin: Java 21 -->
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-compiler-plugin</artifactId>
                <version>3.13.0</version>
                <configuration>
                    <release>${java.version}</release>
                </configuration>
            </plugin>

            <!-- Shade-Plugin: SQLite-Treiber in die JAR packen + umbenennen (relocate) -->
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-shade-plugin</artifactId>
                <version>3.6.0</version>
                <executions>
                    <execution>
                        <phase>package</phase>
                        <goals>
                            <goal>shade</goal>
                        </goals>
                        <configuration>
                            <createDependencyReducedPom>false</createDependencyReducedPom>
                            <relocations>
                                <!-- SQLite umziehen, damit es nicht mit anderen Plugins kollidiert -->
                                <relocation>
                                    <pattern>org.sqlite</pattern>
                                    <shadedPattern>eu.bieder.bigmc.libs.sqlite</shadedPattern>
                                </relocation>
                            </relocations>
                        </configuration>
                    </execution>
                </executions>
            </plugin>
        </plugins>

        <resources>
            <!-- Ressourcen filtern, damit ${project.version} in plugin.yml ersetzt wird -->
            <resource>
                <directory>src/main/resources</directory>
                <filtering>true</filtering>
            </resource>
        </resources>
    </build>
</project>
